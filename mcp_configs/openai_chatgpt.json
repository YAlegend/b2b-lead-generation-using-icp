{
  "note": "ChatGPT connects via HTTP. Run the server first:",
  "start_server": "python3 mcp_server.py --http",
  "connect": "Add http://localhost:8000 as a custom tool in ChatGPT settings",
  "env": {
    "OPENAI_API_KEY": "your-openai-key",
    "OPENAI_MODEL": "gpt-4o-mini"
  }
}
